apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: linkwork-prod

resources:
  - ../../base
  - patches/ingress.yaml
  - patches/hpa.yaml

patches:
  - path: patches/backend-patch.yaml
  - path: patches/env-patch.yaml
